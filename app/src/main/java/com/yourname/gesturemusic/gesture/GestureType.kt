package com.yourname.gesturemusic.gesture

enum class GestureType {
    /** Deliberate gesture that arms media control for a short period. */
    ACTIVATE,
    NEXT_TRACK,
    PREVIOUS_TRACK,
    PLAY_PAUSE
}
